extern int
exists2(
    int sock, // INPUT 
    const char * const name, // INPUT 
    bool *ptr_x
    );
extern int
exists1(
    int sock,
    const char * const val,
    bool *ptr_x
    );
extern int
is_vector(
    int sock, // INPUT 
    const char * const name, // INPUT 
    bool *ptr_x
    );
